const labelStyle = 'text-[18px] font-semibold'
const inputContainer = 'relative mt-2'
const formStyle = 'px-8 pb-8 w-full flex flex-col'
const iconStyle = 'absolute inset-y-0 start-0'
const inputStyle = 'block outline-none border-b border-black w-full pl-8'
const alertTextStyle = 'text-xs text-red-500 my-1'
const buttonStyle = 'text-xl font-semibold text-white rounded-lg py-2 mt-4 bg-primary'

export {
    labelStyle,
    inputContainer,
    formStyle,
    iconStyle,
    inputStyle,
    alertTextStyle,
    buttonStyle
}